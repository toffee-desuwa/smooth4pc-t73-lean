\begin{remark}[较早假设登记表]\label{sec:retired-assumptions}
早期项目草稿将未解决的候选几何汇总为五个假设 A1--A5（实际表示、系数比较、全态余锥、球面映射与有理闭包）。
本文接口为假设~\ref{hyp:P0}--\ref{hyp:P3}，以及不可居 Lean 域
\texttt{ExternalGeometry} 与 \texttt{CSExternalGeometry}。
A1--A5 均不作为定理~\ref{thm:joined} 的前提。
对 Johnson 替换，A1 对应 P0 与 P3/E13，A2--A3 对应 C，A4 对应 S；这些均为 \Open。A5 的计算部分由 P3/E12 连同所引 MWW 命题~3.4 与推论~3.5 解除。
\end{remark}
